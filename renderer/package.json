{
  "name": "murripple-renderer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm run bundle && node --test",
    "bundle": "esbuild src/main.js --bundle --format=iife --global-name=murRippleApp --outfile=dist/bundle.js",
    "test:unit": "node --test test/timeline.test.mjs test/dsp.test.mjs test/palette.test.mjs test/clock.test.mjs test/glow.test.mjs test/audio.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "playwright": "^1.62.1"
  }
}
